interface ServerSideBarProps {
    serverId : string;
}

export const ServerSidebar = async ({serverId} : ServerSideBarProps) => {

     
    return (
        <div>
            Server side bar
        </div>
    )
}